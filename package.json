{
  "name": "cubacadabra-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:game": "sh scripts/sync_first_game.sh",
    "dev": "npm run sync:game && npm run build:wasm && npm run build:renderer && vite",
    "build:wasm": "sh ../rust/scripts/build_wasm.sh",
    "build:renderer": "sh ../rust/scripts/build_web_renderer.sh",
    "build": "npm run sync:game && npm run build:wasm && npm run build:renderer && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
